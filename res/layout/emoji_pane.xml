<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:hardwareAccelerated="false">

  <!-- Search bar -->
  <LinearLayout
      android:id="@+id/emoji_search_bar"
      android:layout_width="match_parent"
      android:layout_height="40dp"
      android:orientation="horizontal"
      android:gravity="center_vertical"
      android:paddingStart="8dp"
      android:paddingEnd="4dp"
      android:background="?attr/colorKey">

    <EditText
        android:id="@+id/emoji_search_input"
        android:layout_width="0dp"
        android:layout_height="32dp"
        android:layout_weight="1"
        android:hint="Search emoji..."
        android:textSize="14sp"
        android:textColor="?attr/colorLabel"
        android:textColorHint="?attr/colorSubLabel"
        android:background="@android:color/transparent"
        android:inputType="text"
        android:imeOptions="actionSearch|flagNoExtractUi"
        android:maxLines="1"
        android:singleLine="true"
        android:paddingStart="8dp"
        android:paddingEnd="8dp"
        android:importantForAutofill="no" />

    <ImageButton
        android:id="@+id/emoji_search_clear"
        android:layout_width="32dp"
        android:layout_height="32dp"
        android:src="@android:drawable/ic_menu_close_clear_cancel"
        android:background="?android:attr/selectableItemBackgroundBorderless"
        android:contentDescription="Clear search"
        android:visibility="gone"
        android:padding="4dp" />

  </LinearLayout>

  <!-- Category buttons -->
  <tribixbite.cleverkeys.EmojiGroupButtonsBar
      android:id="@+id/emoji_group_buttons"
      android:orientation="horizontal"
      android:layout_width="match_parent"
      android:layout_height="wrap_content"/>

  <!-- Emoji grid -->
  <FrameLayout
      android:layout_width="match_parent"
      android:layout_height="@dimen/emoji_grid_height">

    <tribixbite.cleverkeys.EmojiGridView
        android:id="@+id/emoji_grid"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:numColumns="auto_fit"
        android:columnWidth="45sp"
        android:background="?attr/colorKeyboard"/>

    <!-- No results message (hidden by default) -->
    <TextView
        android:id="@+id/emoji_no_results"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center"
        android:text="No emoji found"
        android:textColor="?attr/colorSubLabel"
        android:textSize="16sp"
        android:visibility="gone"
        android:background="?attr/colorKeyboard"/>

  </FrameLayout>

  <!-- Bottom row keyboard -->
  <tribixbite.cleverkeys.Keyboard2View
      layout="@xml/emoji_bottom_row"
      android:layout_width="match_parent"
      android:layout_height="wrap_content"
      android:background="?attr/emoji_key_bg"/>

</LinearLayout>
