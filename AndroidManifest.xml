<?xml version="1.0" encoding="utf-8"?>
<!-- Hardware acceleration enabled for better rendering performance -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android" android:hardwareAccelerated="true">
  <uses-permission android:name="android.permission.VIBRATE"/>
  <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
  <uses-permission android:name="android.permission.RECORD_AUDIO"/>
  <uses-permission android:name="android.permission.INTERNET"/>

  <!-- Storage permissions - deprecated on Android 11+ (API 30+), use scoped storage instead -->
  <!-- These are only needed for Android 10 and below -->
  <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="29"/>
  <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="29"/>

  <!-- For Android 11+ (API 30+), use scoped storage via MediaStore or SAF (Storage Access Framework) -->
  <!-- No explicit permission needed for app-specific directories (getExternalFilesDir()) -->

  <application android:label="@string/app_name" android:allowBackup="true" android:icon="@mipmap/ic_launcher" android:hardwareAccelerated="true">
    <service android:name="tribixbite.keyboard2.CleverKeysService" android:label="CleverKeys Neural Keyboard" android:permission="android.permission.BIND_INPUT_METHOD" android:exported="true" android:directBootAware="true">
      <intent-filter>
        <action android:name="android.view.InputMethod"/>
      </intent-filter>
      <meta-data android:name="android.view.im" android:resource="@xml/method"/>
    </service>

    <activity android:name="tribixbite.keyboard2.SettingsActivity" android:icon="@mipmap/ic_launcher" android:label="@string/app_name" android:theme="@style/settingsTheme" android:exported="true" android:directBootAware="true">
      <intent-filter>
        <action android:name="android.intent.action.MAIN"/>
      </intent-filter>
    </activity>

    <activity android:name="tribixbite.keyboard2.LauncherActivity" android:icon="@mipmap/ic_launcher" android:theme="@style/appTheme" android:exported="true" android:directBootAware="true">
      <intent-filter>
        <action android:name="android.intent.action.MAIN"/>
        <category android:name="android.intent.category.LAUNCHER"/>
      </intent-filter>
    </activity>

    <activity android:name="tribixbite.keyboard2.SwipeCalibrationActivity" android:label="@string/pref_swipe_calibration_title" android:theme="@style/settingsTheme" android:exported="false" android:directBootAware="true"/>

    <activity android:name="tribixbite.keyboard2.NeuralSettingsActivity" android:label="Neural Settings" android:theme="@style/settingsTheme" android:exported="false" android:directBootAware="true"/>

    <activity android:name="tribixbite.keyboard2.NeuralBrowserActivity" android:label="Neural Model Browser" android:theme="@style/settingsTheme" android:exported="false" android:directBootAware="true"/>
  </application>

  <!-- To query enabled input methods for voice IME detection -->
  <queries>
    <intent>
      <action android:name="android.view.InputMethod" />
    </intent>
  </queries>
</manifest>
